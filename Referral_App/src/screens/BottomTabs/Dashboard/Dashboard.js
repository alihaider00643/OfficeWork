import React from "react";
import { Text, View } from "react-native";
import { WP, appIcons, colors, family, size } from "_utilities";
const Home = ({ params }) => (
  <View>
    <Text style={{ color: colors.p1 }}>Home</Text>
  </View>
);

export default Home;
